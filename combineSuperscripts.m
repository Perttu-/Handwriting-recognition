function newBoundingBoxes = combineSuperscripts(centroids)
%combine superscripts and subscripts with main character body. Possibly the
%size difference can be used to determine sub/superscript element of the
%character. Also with centroid distance.
    for i=1:length(centroids)
        boundingBox
    end
    
end