%plotting stuff
p1 = plot([resultStruct.AvgAccuracy]);
set(p1,'linewidth',2);
ax.XTick = [1:20];
ax.YTick = [0.4:0.05:1];
title('Test results of n1 parameter test')
xlabel('Tested value')
ylabel('Detection accuracy')

p1 = plot([resultStruct.AvgPreProcessingTime])
set(p1,'linewidth',2)
ax.XTick = [1:20];
title('Pre-processing time of n1 parameter test')
xlabel('Tested value')
ylabel('Pre-processing time in seconds')
